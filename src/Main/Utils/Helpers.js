import * as THREE from 'three'

export default class Helpers{
    
    constructor(scene){

        // AxesHelper removed - not needed for game
        // GridHelper removed - Environment.js already includes a larger GridHelper (200x200)
    }
}
